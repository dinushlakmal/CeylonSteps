#!/bin/bash
sed -i 's/Toast.makeText(context, "Initiating $displayTitle to Google Drive...", Toast.LENGTH_SHORT).show()/val account = com.google.android.gms.auth.api.signin.GoogleSignIn.getLastSignedInAccount(context)\n                                        if (account == null) {\n                                            android.widget.Toast.makeText(context, "Please Sign In to Google first.", android.widget.Toast.LENGTH_SHORT).show()\n                                        } else {\n                                            isLoading = true\n                                            scope.launch {\n                                                val result = com.example.util.GoogleDriveBackupEngine.performBackup(\n                                                    context, account, tier, trips, journeys\n                                                ) { progress, message ->\n                                                    \/\/ progress handling if needed\n                                                }\n                                                lastResult = result\n                                                isLoading = false\n                                                android.widget.Toast.makeText(context, result.message, android.widget.Toast.LENGTH_SHORT).show()\n                                            }\n                                        }/g' app/src/main/java/com/example/ui/components/BackupRestoreDialog.kt
